apiVersion: v1
kind: Secret
metadata:
  name: leaveflow-secrets
type: Opaque
stringData:
  # These values are intentionally local-demo credentials. Replace them before
  # using this manifest outside a disposable development cluster.
  POSTGRES_DB: leaveflow
  POSTGRES_USER: leaveflow
  POSTGRES_PASSWORD: leaveflow
  DJANGO_SECRET_KEY: leaveflow-local-kubernetes-secret-change-me
